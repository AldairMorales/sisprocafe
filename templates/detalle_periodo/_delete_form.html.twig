<form method="post" action="{{ path('detalle_periodo_delete', {'id': detalle_periodo.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ detalle_periodo.id) }}">
    <button class="btn">Delete</button>
</form>
