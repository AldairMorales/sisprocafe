{% import 'macro/action.html.twig' as action %}

{{ action.frm_start(form) }}
    {{ action.frm_column(
        action.frm_column_field(form.nombre),
        null,
        null,
        null,
        2
    ) }}
    {{ action.frm_column(
        action.frm_column_field_collection(form.detalles),
        null,
        null,
        null,
        2
    ) }}
    {{ action.frm_column(
        action.frm_column_btn(button_label|default('Guardar')),
        null,
        null,
        null,
        3
    ) }}
{{ action.frm_end(form) }}