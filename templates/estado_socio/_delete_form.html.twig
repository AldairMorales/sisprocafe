<form method="post" action="{{ path('estado_socio_delete', {'id': estado_socio.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ estado_socio.id) }}">
    <button class="btn">Delete</button>
</form>
