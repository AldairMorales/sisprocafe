<form method="post" action="{{ path('pagoAcopio_delete', {'id': pagoAcopio.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pagoAcopio.id) }}">
    <button class="btn">Delete</button>
</form>
