<form method="post" action="{{ path('precioAcopio_delete', {'id': precioAcopio.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ precioAcopio.id) }}">
    <button class="btn">Delete</button>
</form>
